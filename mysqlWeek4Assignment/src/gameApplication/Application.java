package gameApplication;

// main method lives here
public class Application {
	
	public static void main(String[] args) {
		// instantiate an instance of the menu
		Menu menu = new Menu();
		// method that actually start running the app
		menu.start();
	}
}
